diff --git a/configure b/configure
index 853bf05e..1866d324 100755
--- a/configure
+++ b/configure
@@ -774,8 +774,8 @@ cat << EOF
 # This version of config.mak was generated by:
 # $cmdline
 # Any changes made here will be lost if configure is re-run
-AR = ${AR:-\$(CROSS_COMPILE)ar}
-RANLIB = ${RANLIB:-\$(CROSS_COMPILE)ranlib}
+AR = ${AR:-\$(CROSS_COMPILE)gcc-ar}
+RANLIB = ${RANLIB:-\$(CROSS_COMPILE)gcc-ranlib}
 ARCH = $ARCH
 SUBARCH = $SUBARCH
 ASMSUBARCH = $ASMSUBARCH
diff --git a/crt/crt1.c b/crt/crt1.c
index 8fe8ab5d..802ff916 100644
--- a/crt/crt1.c
+++ b/crt/crt1.c
@@ -11,6 +11,7 @@ weak void _fini();
 int __libc_start_main(int (*)(), int, char **,
 	void (*)(), void(*)(), void(*)());
 
+__attribute__((__used__))
 void _start_c(long *p)
 {
 	int argc = p[0];
